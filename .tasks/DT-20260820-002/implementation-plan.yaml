type: implementation_plan
task_id: DT-20260820-002
objective: 在 dlt-simulator 的 generator.py compute_weights 内新增 mean_reversion 遗漏回归策略，并同步 SKILL.md 文档
repository: /root/.openclaw/workspace/_ref-tts-public/dlt-simulator
architecture_summary: 保持 compute_weights 单入口 if/elif 分支架构不变，新增 mean_reversion 分支机构（复用 front_last_seen/back_last_seen）
reuse:
  - component: "compute_weights 已算好的 front_last_seen / back_last_seen"
    reason: "避免重复扫描历史数据，遗漏值已就绪，直接消费"
  - component: "generate_pool / rank_candidates / filter_overlap 完整链路"
    reason: "候选生成与评分无需改动，新策略只影响权重输入"
modify:
  - component: "generator.compute_weights"
    reason: "新增 mean_reversion elif 分支（遗漏值越大权重越高），不改现有 balanced/hot/cold/trend 分支"
  - component: "SKILL.md『选号策略』表 + 命令说明"
    reason: "文档与代码同步，说明新策略及其与 cold 的区别"
create:
  - component: "无新增文件"
    reason: "沿用现有单文件分支架构，不引入策略插件系统（最小改动原则）"
steps:
  - step: "实现 compute_weights 的 mean_reversion 分支"
    owner: developer
    dependencies: []
    acceptance_criteria:
      - "weight = f(miss)，miss（遗漏期数）越大权重越高，前区/后区均覆盖"
      - "复用 front_last_seen/back_last_seen，不重复扫描数据"
      - "function 可 import 且 py_compile 通过"
  - step: "更新 SKILL.md 选号策略表 + 命令说明"
    owner: developer
    dependencies:
      - "实现 compute_weights 的 mean_reversion 分支"
    acceptance_criteria:
      - "选号策略表新增 mean_reversion 一行，注明与 cold 区分"
      - "命令区说明 --strategy mean_reversion 可用"
  - step: "回归验证现有四策略 + mean_reversion 冒烟"
    owner: developer
    dependencies:
      - "实现 compute_weights 的 mean_reversion 分支"
    acceptance_criteria:
      - "balanced/hot/cold/trend 对同一 draws 输出与改动前一致"
      - "python3 generator.py --strategy mean_reversion 跑通不崩"
      - "有测试证据（命令 + 输出 + 回归对比）"
testing: "冒烟：python3 -m py_compile scripts/generator.py；CLI：python3 scripts/generator.py --strategy mean_reversion；回归：对固定 draws 分别跑四个老策略对比改动前后输出"
validation: "Minimal Validator（changed_files / 测试执行 / 测试通过 / acceptance 满足 / diff 合理）"
review: "提交前由 Repository Reviewer 走 10 Gate 审核（Phase 2 只到 Implementation Plan，不进入真正开发）"
rollback: "git revert 单 commit 回退；改动集中在一个 elif 分支 + 文档一行，无配置/数据/多模块影响"
definition_of_done: "mean_reversion 策略可经 --strategy 调用且权重随遗漏递增；现有四策略回归输出不变；SKILL.md 已同步；有可复现测试证据；未 push"
